;; OS X specific key mapping...
;(setq mac-command-modifier 'ctrl)

;(setq mac-pass-command-to-system nil)

;; Turn the menu back on (the menu is ALWAYS there in OS X)
(if (fboundp 'menu-bar-mode) (menu-bar-mode t))
